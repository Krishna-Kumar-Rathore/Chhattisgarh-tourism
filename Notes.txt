curl -X POST http://localhost:5000/api/auth/create-admin \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Admin User",
    "email": "admin@chhattisgarh.com",
    "password": "admin123",
    "adminSecret": "your_super_secret_admin_key_here"
  }'


  # Terminal 1 - Backend
cd backend && npm run dev

# Terminal 2 - Frontend  
cd frontend && npm start